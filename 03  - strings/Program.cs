/*
using System;

namespace Loop{
    class Program{
        static void Main(string[] args){
          string name = "coder";
          string[] array = {"coding","C#"};
          Console.WriteLine(name.Length);    
          Console.WriteLine(name.EndsWith("er"));    
          Console.WriteLine(name.StartsWith("co"));    
          Console.WriteLine(name.StartsWith('c'));    
          Console.WriteLine(name.Contains('z'));    
          Console.WriteLine(name.IndexOf("d"));    
          Console.WriteLine(name.Replace("c","C"));    
          Console.WriteLine(string.IsNullOrEmpty(name));
          Console.WriteLine(string.Join("",array));

        }
    }
}

*/

